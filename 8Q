#include <iostream>
#include <vector>
#include <windows.h>
using namespace std;
void da();
int p[8];
int y=0,x=0;
bool ex=false;
int t[8][8];
int main()
{
    for(int i=0;i<8;i++)p[i]=11;
    while((x<8)||(p[0]<8))
    {
        ex=false;
        while(ex==false)
        {
            p[x]=y;
            da();
            for(int i=0; i<8; i++)for(int j=0; j<8; j++)t[i][j]=0;
            for(int a=0; a<=x&&ex==false; a++)
            {
                t[a][p[a]]=1;
                for(int i=0; i<8&&ex==false; i++)
                {
                    for(int c=0; c<8&&ex==false; c++)if((p[c]==p[a])&&(c==i)&&(c!=a))ex=true;
                    t[i][p[a]]=1;
                }
                for(int i=a,j=p[a]; i<8&&j<8&&ex==false; i++,j++)
                {
                    for(int c=0; c<8&&ex==false; c++)if((p[c]==j)&&(c==i)&&(c!=a))ex=true;
                    t[i][j]=1;
                }
                for(int i=a,j=p[a]; i>=0&&j<8&&ex==false; i--,j++)
                {
                    for(int c=0; c<8&&ex==false; c++)if((p[c]==j)&&(c==i)&&(c!=a))ex=true;
                    t[i][j]=1;
                }
            }
            system("CLS");
            for(int i=0; i<8; i++){for(int j=0; j<8; j++)if(p[j]==i)cout<<1;else cout<<0;cout<<'\n';}
            cout<<'\n';
            for(int i=0; i<8; i++){for(int j=0; j<8; j++)if(t[j][i]==1)cout<<1;else cout<<0;cout<<'\n';}
            if(ex==false)
            {
                y=0;
                x++;
            }
        }
        y++;
        p[x]++;
        if(y==9)
        {
            p[x]=11;
            x--;
            y=p[x]+1;
            p[x]++;
        }
    }
    if(p[0]==8)
        cout<<"Nu se poate";
    else
        for(int i=0; i<8; i++)
            cout<<'('<<i<<' '<<p[i]<<') ';
    return 0;
}
void da()
{

}
